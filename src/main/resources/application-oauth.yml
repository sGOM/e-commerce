# 소셜 로그인 설정. 'oauth' 프로파일을 활성화하고 환경변수로 시크릿을 주입한 뒤 사용한다.
#   예) SPRING_PROFILES_ACTIVE=local,oauth  +  GOOGLE_CLIENT_ID, GOOGLE_CLIENT_SECRET ...
# (base 설정에 등록정보를 두면 시크릿이 없을 때 부팅이 실패하므로 별도 프로파일로 분리)
spring:
  security:
    oauth2:
      client:
        registration:
          google:
            client-id: ${GOOGLE_CLIENT_ID}
            client-secret: ${GOOGLE_CLIENT_SECRET}
            scope:
              - profile
              - email
          naver:
            client-id: ${NAVER_CLIENT_ID}
            client-secret: ${NAVER_CLIENT_SECRET}
            client-name: Naver
            authorization-grant-type: authorization_code
            redirect-uri: "{baseUrl}/login/oauth2/code/naver"
            scope:
              - name
              - email
          kakao:
            client-id: ${KAKAO_CLIENT_ID}
            client-secret: ${KAKAO_CLIENT_SECRET}
            client-name: Kakao
            authorization-grant-type: authorization_code
            redirect-uri: "{baseUrl}/login/oauth2/code/kakao"
            client-authentication-method: client_secret_post
            scope:
              - profile_nickname
              - account_email
        provider:
          naver:
            authorization-uri: https://nid.naver.com/oauth2.0/authorize
            token-uri: https://nid.naver.com/oauth2.0/token
            user-info-uri: https://openapi.naver.com/v1/nid/me
            user-name-attribute: response
          kakao:
            authorization-uri: https://kauth.kakao.com/oauth/authorize
            token-uri: https://kauth.kakao.com/oauth/token
            user-info-uri: https://kapi.kakao.com/v2/user/me
            user-name-attribute: id

# 소셜 로그인 성공/실패 후 프론트엔드 이동 경로
app:
  oauth2:
    success-redirect-uri: ${OAUTH_SUCCESS_REDIRECT_URI:/}
    failure-redirect-uri: ${OAUTH_FAILURE_REDIRECT_URI:/login}
